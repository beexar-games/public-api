# Timings and retries

<!-- GENERATED by backend/tools/gen-integration-skill from
     backend/pkg/walletbudget/walletbudget.go and
     backend/gateway-service/internal/service/operator/retry.go.
     Do not edit: `make sdk-skill` regenerates it and CI fails on a diff. -->

This is the part of the contract nobody reads until production.

| Budget | Value | Source |
|---|---|---|
| `OperatorAttemptTimeout` | 15s | `pkg/walletbudget/walletbudget.go` |
| `OperatorRetryEnvelope` | 30s | `pkg/walletbudget/walletbudget.go` |
| `GatewayWalletDetachedBudget` | 50s | `pkg/walletbudget/walletbudget.go` |
| `GameWalletCallBudget` | 70s | `pkg/walletbudget/walletbudget.go` |
| `RedeliveryMargin` | 5s | `pkg/walletbudget/walletbudget.go` |
| `BetWin attempts` | 4 | `gateway-service/internal/service/operator/retry.go` |
| `Finish attempts` | 5 | `gateway-service/internal/service/operator/retry.go` |

## What it means for your handler

- Answer within the **attempt timeout**. Past it the platform gives up on
  that attempt and may refund the round.
- A **5xx or a timeout is retried** with the **same `id_provider`**, inside
  the retry envelope. Your handler must therefore be idempotent — that is
  not advice, it is the only thing keeping a retry from double-charging.
- **Not retried:** insufficient funds, bet limits, bad requests and signature
  failures. Those are terminal answers, and the player sees the result.
- Do the heavy work outside the handler. Anything slower than the attempt
  timeout should already have happened before the callback arrives.
